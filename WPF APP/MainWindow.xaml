<Window x:Class="WPF_APP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_APP"
        mc:Ignorable="d"
        Title="Club Managment DashBord" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="267*"/>
            <ColumnDefinition Width="147*"/>
            <ColumnDefinition Width="266*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Liste des Joueurs" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" Grid.ColumnSpan="4"/>

        <DataGrid x:Name="dgPlayers" 
                  Grid.Row = "1"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  AlternatingRowBackground="#F2F2F2"
                  BorderBrush="#DDDDDD"
                  HeadersVisibility="All" Grid.ColumnSpan="4" SelectionChanged="dgPlayers_SelectionChanged">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding PersonID}" Width="50"/>
                <DataGridTextColumn Header="Prénom" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Nom" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Téléphone" Binding="{Binding Phone}" Width="100"/>
                <DataGridTextColumn Header="Catégorie" Binding="{Binding CategoryID}" Width="100"/>

                <DataGridCheckBoxColumn Header="Actif" Binding="{Binding isActive}" Width="60"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Window>
